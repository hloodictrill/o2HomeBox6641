HomeBox 6641 Consumer Release Notes

This release contains all of the files needed to build a HomeBox 6641 DSL Router
image.  To build an image, enter:

# make PROFILE=<profile_name>

For example,

# make PROFILE=96368GW

The final image is located in the images subdirectory.  This image can be
specified in the HomeBox 6641 Web interface image upgrade item in order to
upgrade the DSL router image.

